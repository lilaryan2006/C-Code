#include <stdio.h>
#include <conio.h>
int main()
{
    float s, p, a;
    printf("Enter the Sides of Square\n");
    scanf("%f", &s);
    p = 4 * s;
    a = s * s;
    printf("The Parameter of Square is %f and the Area of square is %f", p, a)  ;

    return 0;
}
